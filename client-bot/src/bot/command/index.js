const client = require('./client');
const organization = require('./organization');
const points = require('./points');
const reciever = require('./reciever');

module.exports = { client, organization, points, reciever };
